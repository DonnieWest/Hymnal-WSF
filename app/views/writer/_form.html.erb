<%= form_for :writer, url: '/writer' do |f| %>
    <% if @writer.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@writer.errors.count, "error") %> prohibited this song from being saved:</h2>

          <ul>
            <% @writer.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
    <div class="field">
      <%= f.label :name %>
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label :details %>
      <%= f.text_area :details, :value => @writer.details %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
