$modal = $('.modal'),
        $modalBody = $('.modal .modal-body'),
        $modalHeading = $('.modal .modal-heading');
        $modalHeading.html("<p class=\"text-center\"><%=@song.title %></p> <h4 class=\"text-center\"><%=@song.page%></h4>");
        $modalBody.html("<%= escape_javascript(render @song) %>");
        $modal.modal();
